{% extends '_layout.twig' %}

{% block content %}
    {% set book = craft.entries.slug(craft.app.request.getSegment(2)).section('book').one() %}

<main class="main-fourthwing">
    {#  <div class="afb"><img src="{{ image.url }}" alt="book.alt"></div>#}
    <div class="inhoud">
        <h1>{{ book.title }}</h1>
        <h3>{{ book.bookAuthor}}</h3>
        <p>{{ book.description }}</p>
    </div>
    <div class="prijs">
        <h2>{{ book.price|money('be') }}</h2>
        <div class="aantal">
            <button>&laquo;</button>
            <a class="cijfer" href="#"></a>
            <button>&raquo;</button>
        </div>
        <a class="but">
            <button id="buy">Add to cart</button>
        </a>
        <p></p>

    </div>
</main>

{% endblock %}